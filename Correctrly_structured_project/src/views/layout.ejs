<!DOCTYPE html>
<html lang="en">

<head>
    
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png" href="/images/Icon.png">
<!DOCTYPE html>
<html lang="<%= language %>">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png" href="/images/Icon.png">
    <script nonce="<%= nonce %>">
        window.translations = {
            en: <%- JSON.stringify(i18n.getCatalog('en')) %>,
            lv: <%- JSON.stringify(i18n.getCatalog('lv')) %>,
            de: <%- JSON.stringify(i18n.getCatalog('de')) %>,
            ru: <%- JSON.stringify(i18n.getCatalog('ru')) %>
        };
        window.currentLanguage = '<%= language %>';
    </script>
</head>

<body>
    
    <%- include('./partials/header') %> <!-- This will include your header -->
    <main>
        <%- body %> <!-- This will be replaced with the page content -->
    </main>

</body>

</html>
